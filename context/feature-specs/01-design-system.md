Read `AGENTS.md` before starting. 

We are adding the design system and UI primitive components. 

Install and configure `shadnc/ui`. 

Add these shadcn componnets: 
- Button
- Card
- Input
- Tabs
- Textarea
- Scorll Area

Do not modify the generated `components/ui/*` files after installation.


Also install `lucide-ract`.


Create `lib/utils.ts` with the resuable `cn()` helper for merging Tailwind classes.


Ensure all compenents match the existing dark theme in `globals.css`.


### Check when done

- All components import without errors
- `cn()` works properly 
- no default light styling appears



